<!DOCTYPE html>
<html>
	<head>
		<% include ../partials/header.ejs %>
	</head>
	<body>
		<% include ../partials/nav.ejs %>

		<div class="container">
			<h2>Users:</h2>

			<ul>
				<% users.forEach(function(u) { %>
					<li><%= u.id %> - <%= u.name %></li>
				<% }); %>
			</ul>
		</div>
	</body>
</html>
